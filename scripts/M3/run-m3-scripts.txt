# (only needed once per shell)
$env:GCP_PROJECT_ID = "recommender-api-476122"
$env:GCP_REGION     = "us-central1"

# Setup
.\scripts\M3\bootstrap.ps1 -Project recommender-api-476122 -Region us-central1

# Build image and push to Artifact Registry
.\scripts\M3\build.ps1 -Tag m3-final

# Deploy to Cloud Run
.\scripts\M3\deploy.ps1

# Verify endpoints
.\scripts\M3\smoke.ps1
